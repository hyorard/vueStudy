function home(){
    const bestPlayer='Gerrard';
    // bestPlayer='henderson';
    // error

    console.log(bestPlayer);

    // 상수는 const 그러나 불변은 아님! 값 재할당만 불가능.
    // var는 상위? 쪽에 있으면 계속 참조 가능
    // 참조하고 싶지 않을 때에 let 사용
}